services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: apu-portfolio:latest
    container_name: apu-portfolio
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
